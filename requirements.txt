# PRODUCTION MINIMAL - Railway Deployment
pydantic==2.5.3
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
email-validator==2.1.0
httpx==0.25.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2
cryptography==41.0.7
python-dotenv==1.0.0
requests==2.31.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
# starlette==0.27.0  # Compatible with FastAPI 0.104.1

# REMOVED for compatibility:
# - langchain (pulls numpy, pandas, heavy ML stack)
# - pandas, numpy, alembic, stripe, sentry, redis, celery

# click is now installed as uvicorn dependency
