# PRODUCTION MINIMAL - Render Deployment
# Python 3.12 - Zero C extensions, zero numpy/pandas dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.1
cryptography==41.0.7
python-dotenv==1.0.0
httpx==0.25.0
requests==2.31.0
email-validator==2.1.0

# REMOVED for Python 3.14 compatibility:
# - langchain (pulls numpy, pandas, heavy ML stack)
# - sqlalchemy (works but optional for MVP with in-memory data)
# - pandas, numpy, alembic, stripe, sentry, redis, celery
