# PRODUCTION MINIMAL - Render Deployment
# FORCE Pydantic V1 and VERY OLD FastAPI for compatibility
pydantic==1.10.13
fastapi==0.89.0
uvicorn[standard]==0.18.0
python-multipart==0.0.5
email-validator==1.3.1
httpx==0.23.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
cryptography==38.0.4
python-dotenv==0.21.0
requests==2.28.2

# Database (optional for MVP)
sqlalchemy==1.4.46

# Force starlette version compatible with old FastAPI
starlette==0.22.0

# REMOVED for compatibility:
# - langchain (pulls numpy, pandas, heavy ML stack)
# - pandas, numpy, alembic, stripe, sentry, redis, celery
