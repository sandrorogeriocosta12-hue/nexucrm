# PRODUCTION MINIMAL - Render Deployment
# FORCE Pydantic V1 for compatibility
pydantic==1.10.13
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
email-validator==2.1.0
httpx==0.25.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.1
cryptography==41.0.7
python-dotenv==1.0.0
requests==2.31.0

# Database (optional for MVP)
sqlalchemy==2.0.23

# REMOVED for compatibility:
# - langchain (pulls numpy, pandas, heavy ML stack)
# - pandas, numpy, alembic, stripe, sentry, redis, celery
