{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "PORT=8000 uvicorn app_test:app --host 0.0.0.0 --port 8000",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}